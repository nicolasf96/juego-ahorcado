name: Principal

on:
  push:
    branches:
      - main


jobs:
  ahorcado_tests:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 16
    - run: npm ci
    - run: npm build --if-present
    - run: npm test


